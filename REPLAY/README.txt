Six mock round tapes: r1-replay.json … r6-replay.json.

Board snapshot keys MUST match gameUtils.territories in js/core.js (e.g. alaska, ontario, northwest_territory).
Generic names like "North" / "South" do not exist on the SVG map — replay runs but shows no troop markers.

Copy these into C:\RISQUE\save\replay (or your SAVE\REPLAY folder). Filenames must include "replay" for folder scan.

sessionKey: mock-chain-001 on all six.
